---
title: "SignalP Tables Sorted by BitScore - Distinct "
output: 
  html_document:
    css: style.css
    toc: true      # Enable Table of Contents
    toc_depth: 3 
params:
  Table10: NULL
  Table11: NULL
  name: NULL
---
<!-- get current date --> 
**`r params$name`**  
  `r Sys.Date()`

```{r, echo = FALSE, message=FALSE, warning= FALSE, results = 'hide'}
library(kableExtra)
library(DT)
library(dplyr)
library(knitr)
```

```{r}
#read in our table of distinct transcripts and the table of distinct hits 
Table10 <- read.csv(params$Table10)
Table11 <- read.csv(params$Table11)

```

# Top 100 Sorted by Bit Score, Distinct Transcripts

```{r}
#display table of distinct transcripts 
Table10 %>%
  kable("html", 
        caption = "Top 100 Sorted by Bit Score, Distinct Transcripts", 
        booktabs = TRUE, 
        escape = FALSE) %>%  # Escape = FALSE to render HTML
  kable_styling(bootstrap_options = c("striped", "hover", "condensed"), 
                full_width = FALSE, 
                position = "center") %>% 
  column_spec(1, width = "4cm", bold = TRUE, color = "white", background = "#4CAF50") %>% 
  row_spec(0, bold = TRUE, font_size = 14, color = "white", background = "#4CAF50") %>% 
  footnote(general = "Table shows the top 100 rows of KBT sorted by bit score, with hyperlinks to UniProt entries.", 
           general_title = "Note: ", 
           footnote_as_chunk = TRUE)
```

# Top 100 Sorted by Bit Score, Distinct Hits
```{r}
#display table of distinct hits
Table11 %>%
  kable("html", 
        caption = "Top 100 Sorted by Bit Score, , Distinct Transcripts", 
        booktabs = TRUE, 
        escape = FALSE) %>%  # Escape = FALSE to render HTML
  kable_styling(bootstrap_options = c("striped", "hover", "condensed"), 
                full_width = FALSE, 
                position = "center") %>% 
  column_spec(1, width = "4cm", bold = TRUE, color = "white", background = "#4CAF50") %>% 
  row_spec(0, bold = TRUE, font_size = 14, color = "white", background = "#4CAF50") %>% 
  footnote(general = "Table shows the top 100 rows of KBT sorted by bit score, with hyperlinks to UniProt entries.", 
           general_title = "Note: ", 
           footnote_as_chunk = TRUE)
```

